import type { GrammarRule } from "../../shared/types";

export const GrammarEngine = {
  detectRule(_utterances: string[]): GrammarRule | null {
    // Simplified grammar rule detection (placeholder)
    return null;
  },
};
